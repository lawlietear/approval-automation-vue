# 安全调试与部门选择：单位验证交接

本次审查修复绿色版主程序 SHA256：`C9C12536E00FBA9DF0DA0E10A75983950BB0F7B71E1F0E0A7CF559FDEB09733E`。
执行组件 ApprovalRunner.exe SHA256：`B773E0656447E5DB8DB14EAC0ECD25CB86E2F49B6319D4DBFECE7F606EAF68C8`。
审查修复后通过64项Python全量回归，新增一项后15项调试定向回归通过；13项Rust测试及模拟界面/打包组件验证通过。没有连接真实单位审批页面。

本轮追加核验：部门弹窗在主页面、详情在iframe时应正常提取；缺少最终确认配置必须报错；正式提交前部门被改变必须停止，不点最终确定、不登记。上述后两项仅在模拟页面验证，不能为测试去批准真实事项。debug_result完整记录不再重复写入诊断日志，原先已生成的日志仍按5天保留规则处理。

## 本轮边界

2026-09-26 交接资料已确认：中间确定可能直接提交，不能因用户填写多步就自动点击。
本版调试只做字段提取、当前窗口读取和 Playwright trial 可点击性检查，**所有真实点击均禁止**；不打开后续审批窗口、不选择 radio、不点击最终确定、不调用登记组件。
trial 可能滚动页面使目标进入视野，但不会发送点击。不采用阻断网络请求的方式冒充安全审批。

## 使用步骤

1. 使用本轮完整 dist_green，保留单位本机 Settings 下的配置。先按原方式连接 Chrome。
2. 在“设置 → 调试与部门”勾选安全调试，选择用户确认的预计步骤数，保存。未确认步骤数不能开启。
3. 返回首页，确认标题显示“安全调试 · 不审批不登记”，OA按钮不可用，再点击核心业务系统。应只出现提取信息和检查结果，不应出现新的审批弹窗或登记记录。
4. 如需读取部门选项，由用户在正常、已授权的业务办理中自行到达部门窗口；不要让 Codex 为采集而点击通过/中间确定。
5. 在设置中点“读取已打开的部门窗口”。应看到本次窗口真实的部门ID与名称。单页且条数/分页状态一致时才允许更新缓存；多页仅显示当前页，提示不完整，不覆盖已保存列表。本轮不自动翻页。
6. 选择正式审批默认部门，按需打开“在首页显示部门选择”，保存。首页修改会立即保存；关闭显示开关只隐藏控件，不清除默认选择。
7. 在调试模式再次运行，所选部门只检查可点击性，单选状态必须保持不变。最终确定始终不点击。

设置保存在本机 Settings/workflow.json，与程序分离；不要把用户配置打进公共分发包。
来源和部门ID会在正式选择时重验，重复名称不会导致点错行。目标不在当前页或来源不匹配时停止，不回退选其他部门。
未选择部门ID时沿用历史配置，不改变已有“风险合规部”的默认行为。

## 正式模式验证必须另行授权

关闭调试并保存后，原有审批和登记恢复。默认部门会用于正式核心审批；不会用于 OA，也不会修改提取记录中的申请部门字段。
真实部门选择的运行行为尚需单位验收：采集报告只验证了 trial，不能将模拟页面测试说成真实运行成功。
只有用户明确授权具体事项后，才允许执行正式审批并检查选中部门及最终流程历史。不要为验证部门选择而批准无授权事项。

## 核验清单

- 一步、多步声明下均无同意/中间确认/最终确认点击；企业微信与 Obsidian 无新增记录。
- 调试结果不应标成审批成功或登记成功。旧 --test-mode 不是安全调试，不要使用。
- 部门选项是当前窗口数据，不是采集报告的两项硬编码；查不到窗口或发现多个窗口应报错停止。
- 默认部门和首页显示开关重启后保留；调试开关开启时命令行 approve 也拒绝执行。
- 部门选择失败不能再次进入审批流程；本轮已修复旧页面检测吞掉执行异常的问题。
- 首页增加可选部门行后，内容较多时可滚动，不扩大默认640高度。

回传脱敏截图、准确时间、检查结果文本、选项页数和单选框前后状态即可。不发送登录参数、Webhook、完整浏览器资料；不用重走审批来补采截图。
